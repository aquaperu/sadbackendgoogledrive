import { SetMetadata, applyDecorators } from '@nestjs/common';

export const Xlsx = ():MethodDecorator => {
   
   return (target: any, propertyKey: string, descriptor: PropertyDescriptor)=>{


    
    return descriptor

   }
   
  };
;
